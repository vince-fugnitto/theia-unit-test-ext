# Theia Test Extension

## Description

The following is an example on how to setup mocha testing for an example `@theia` extension (`hello world`).

## Steps

1. build through `yarn`.
2. test through `yarn test`.
